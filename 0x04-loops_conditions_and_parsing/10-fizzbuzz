B#!/usr/bin/env bash
# This script is displaying "Fizz, Buzz or FizzBuzz"
i=1
while [ $i -le 100 ]
do
	if [[ $((i % 3)) == 0 ]]
	then
	    echo "Fizz"
	elif [[ $((i % 5)) == 0 ]]
	then
	    echo "Buzz"
	elif [[ $((i % 15)) == 0 ]]
	then
	    echo "FizzBuzz"
	else
	    echo "$i"
	fi
	((i++))
done
